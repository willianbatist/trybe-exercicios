let numbers = [2, 3, 6, 7, 10, 1];
let maior;
let index;
let resultado;
function bigValue (numbers) {
    maior = Math.max.apply(null, numbers );
    for (let index of numbers);
    if (index = maior) {
        resultado = index === maior;
    }
    return resultado;
    //for (index = 0; index < numbers.length; index += 1); {
        // console.log(index)
    //}
    
    //console.log(maior === index[numbers])
}
bigValue(numbers)

